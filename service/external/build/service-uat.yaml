apiVersion: v1
kind: Service
metadata:
  labels:
    name: external-uat
    app: external
    environment: uat
    namespace: default
  name: external-uat
  namespace: default
spec:
  ports:
    - name: http
      appProtocol: http
      port: 8000
      targetPort: http
  selector:
    name: external-uat
