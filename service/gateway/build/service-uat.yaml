apiVersion: v1
kind: Service
metadata:
  labels:
    name: gateway-uat
    app: gateway
    environment: uat
    namespace: default
  name: gateway-uat
  namespace: default
spec:
  ports:
    - name: http
      appProtocol: http
      port: 8000
      targetPort: http
  selector:
    name: gateway-uat
