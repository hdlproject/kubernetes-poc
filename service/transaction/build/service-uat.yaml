apiVersion: v1
kind: Service
metadata:
  labels:
    name: transaction-uat
    app: transaction
    environment: uat
    namespace: default
  name: transaction-uat
  namespace: default
spec:
  ports:
    - name: http
      appProtocol: http
      port: 8000
      targetPort: http
  selector:
    name: transaction-uat
